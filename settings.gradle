pluginManagement {
    repositories {
        // FIXED: The exact, verified Maven server repository route from your screenshot
        maven {
            url = "https://maven.fabricmc.net/"
        }
        mavenCentral()
        gradlePluginPortal()
    }
}

plugins {
    // Reads your new 1.17-SNAPSHOT version setting perfectly
    id 'fabric-loom' version '1.17-SNAPSHOT'
}

rootProject.name = 'turbowarp-x-mincraft'
